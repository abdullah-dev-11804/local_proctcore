ProctorCore 0.10.0 static validation

- 76 PHP files passed php -l.
- All JavaScript source/build files passed node --check.
- All XML files parsed successfully.
- ML Python files passed py_compile.
- All literal Moodle language-string references resolved.
- No existing database table or field was changed for Sections 1.2/1.3.
- Local and access-plugin dependency versions are matched.

Runtime acceptance testing is still required on the target Moodle/IOMAD site.
The ML thresholds must be calibrated with consented representative data before production.
